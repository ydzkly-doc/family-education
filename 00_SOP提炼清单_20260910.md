# 本次清理动作 → 可提炼进专家 SOP 的经验清单

> 状态：**仅梳理列出，尚未改动 SOP**。请你圈定要采纳的条目后我再动手。
> 现有 SOP 结构：第 0–9 步（均为"内容生产"流程），**完全没有"维护与清理"环节**。

---

## 一、最值得进的：目录清理安全流程（SOP 目前完全缺失）

| # | 经验 | 优先级 | 建议落点 |
|---|---|---|---|
| 1 | **三步安全清理法：只读扫描 → 出报告等确认 → 分批执行**。用户说"清理一下"时，第一轮**只扫描出报告、绝不删**，按 A（可删）/B（先留）/C（需确认）三类归档，等用户圈选 | ⭐⭐⭐ | 新增第 10 步 |
| 2 | **必须走系统回收站**（`send2trash`），禁止 `rm`；分批执行每批 ≤10 项、每批核对 | ⭐⭐⭐ | 第 10 步 |
| 3 | **区分"历史约定"与"过程文件"**：旧系列 `02_发布操作指南.md` 是历史约定**不是垃圾**（新规范才改 .txt）；清理时按"四件套白名单"排除，不能见非标准名就删 | ⭐⭐⭐ | 第 10 步 |
| 4 | **清理前先记清单**（原路径 + 体积），写入 `.workbuddy/_清理清单_日期.md`，便于从回收站找回 | ⭐⭐ | 第 10 步 |
| 5 | **清理前先 git 提交**，把当天工作固化进历史，让后续删除都可回滚 | ⭐⭐ | 第 10 步 |
| 6 | **过程文件"挪"不"删"**：未采用的封面候选图、生图/后处理脚本，移到各系列 `_过程文件/` 保留提示词与逻辑，而非直接删除 | ⭐⭐ | 第 8 步（沉淀与命名） |

---

## 二、踩坑记录（这是最"贵"的一条）

| # | 经验 | 优先级 |
|---|---|---|
| 7 | **⚠️ `send2trash` 在中文路径下会误报失败**：明明删除成功却抛 `FileNotFoundError [WinError 2]` 或 `PermissionError [WinError 5]`。**绝不能信它的返回值**——必须以 `os.path.exists()` 复核工作区真实状态；必须用绝对路径。验证是否真进回收站：解析 `D:\$Recycle.Bin\<SID>\$I*` 元数据（8字节版本+8字节大小+8字节时间+4字节名长+UTF-16原名） | ⭐⭐⭐ |
| 8 | `PowerShell` 的 `New-Object -ComObject Shell.Application` 在本环境被安全策略拦截，不能用来读回收站 | ⭐ |
| 9 | `git rm` 参数过长报 `WinError 206 文件名或扩展名太长` → 用 `--pathspec-from-file=<清单文件>` | ⭐⭐ |

---

## 三、发布包卫生 + 版本库治理

| # | 经验 | 优先级 | 建议落点 |
|---|---|---|---|
| 10 | **发布包内只允许四件套**：`正文_第X篇_*.html` + `01_*.txt` + `02_*.txt` + `封面_第X篇_*.jpg`。任何过程脚本、候选图、`_原始png/` 都不该留在包里 | ⭐⭐⭐ | 第 7 / 9 步自检项 |
| 11 | **`.gitignore` 对已跟踪文件无效**：加规则后必须再 `git rm -r --cached`（**只移除索引、保留本地文件**）才能真正生效。本次清理出 157 个误跟踪文件 | ⭐⭐⭐ | 第 8 步 |
| 12 | **`.gitignore` 标准条目**（本工作空间已落地）：`_backup*/`、`_归档*/`、`_过程文件/`、`_预览/`、`_原始png/`、`_封面候选/` | ⭐⭐ | 第 8 步 |

---

## 四、自检脚本判据打磨（本轮被 6 处误报逼出来的）

| # | 经验 | 优先级 | 建议落点 |
|---|---|---|---|
| 13 | **标题比对必须先归一化**：去中英弯直引号、书名号、括号、分号、空白；旧系列再加"分词位移容忍"（正文分两行致标点位置不同时，去标点后互为子串即算一致） | ⭐⭐⭐ | 第 9 步 |
| 14 | **动作指令检测只扫可见文字**（剥 `<script>/<style>`），并建**豁免表**："点赞越多我凉得越快""分享给你""父母改变百分之一…"等叙事句不算；**豁免词必须长词优先排序**，否则短词先替换会破坏长词边界 | ⭐⭐⭐ | 第 9 步 |
| 15 | **来源声明分三类口径**，自检需分别认：①课程类"扶鹰…非课程原文" ②读书笔记类"整理自《书名》…非原文摘录" ③**原创真实经历类"本文为作者真实经历原创分享…模糊化处理"**（本次把手机危机篇误判过一次） | ⭐⭐⭐ | 第 9 步 |
| 16 | **标题抽取要兼容多种历史写法**：①新系列 `font-size≥20px + 居中`；②旧系列未写字号但 `display:block + bold + 居中`；③单行内多个 `<p>` 连写的情况。取到标题块后遇小字号行即停，避免把坐标行/金句块误当标题 | ⭐⭐ | 第 9 步 |
| 17 | **预览页篇数统计要兼容单双引号** `class='art'`，且只匹配正文容器 `class="art"/"art show"`，**不匹配导航标签 `arttag`**（否则篇数翻倍误报） | ⭐⭐ | 第 9 步 |

---

## 五、存量巡检机制（新增能力）

| # | 经验 | 优先级 | 建议落点 |
|---|---|---|---|
| 18 | **双模式自检**：新系列走**严格模式**（交付门禁），既有系列走 `--legacy` **巡检模式**（放行"02 曾为 .md / 早期包无 01 字段表 / 来源口径旧写法"等历史差异，只报真问题） | ⭐⭐⭐ | 第 9 步 |
| 19 | **交付/修改后用统一脚本一把过**：`工具脚本/系列发布全套自检_selfcheck.py --dir <成果根目录>`，9 项一次跑完，比逐篇目检快且不漏 | ⭐⭐⭐ | 第 9 步 |
| 20 | **跨系列巡检要留报告**：发现的问题分类为"真问题（修）/ 历史写法差异（不动）/ 待确认（问用户）"，**不擅自批量改历史成品**——已发布内容整体重制风险高于收益 | ⭐⭐ | 第 10 步 |

---

## 六、已在上一轮进 SOP 的（本条仅为对照，无需重复）

- 封面 **A0 无人物静物构图优先**、**A2 主体不能太小**（已写入第 7 步封面规则）
- `_预览/` 独立目录防预览写回污染（已在第 8 步）
- 02 指南用 `.txt` 不用 `.md`（已在第 7 步）

---

## 七、我的建议

**最小改动、收益最大**的方案是加一节：

> **第 10 步 · 维护、巡检与清理**
> 含：三步安全清理法（只读扫描→报告→分批走回收站）、`send2trash` 中文路径误报坑、发布包卫生、`.gitignore` 与 git 索引治理、跨系列巡检双模式。

同时把 **#10 / #11 / #13–#19** 这几条**并入第 8、9 步**（属于既有步骤的自然延伸）。

---

## 八、请你圈定

可多选，直接说编号即可：

- **全采纳**（我按第七节方案改：新增第 10 步 + 强化第 8/9 步）
- **只要清理流程**（#1–#9 + #10 + #11 + #20）
- **只要脚本判据**（#13–#19）
- **自选编号**：________

> 状态：清单已列出，**未修改任何 SOP 文件**，等你确认。
