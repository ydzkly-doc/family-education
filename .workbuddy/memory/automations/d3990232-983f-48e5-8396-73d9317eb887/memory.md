# 自动化任务记忆 — 过程文件过期扫描

任务 ID：d3990232-983f-48e5-8396-73d9317eb887
性质：**只读扫描 + 对话内汇报**（不落盘、不删除、不移动）

## 执行记录

### 2026-09-14（首次执行）
- 命令：`python 工具脚本/过程文件过期扫描_scan_stale.py --brief`（系统 python 可用）
- 结果：过程类目录 **61 个 / 237.3 MB**，**超期 0 个** → 本周无需清理
- 分类：A 21个/11.4M｜B 18个/100.4M｜C 20个/125.4M｜D 2个/0.1M

### 2026-09-21（第二次执行）
- 命令：`--brief` + **无参完整版**（因汇报必须列具体路径，brief 只给汇总）
- 结果：过程类目录 **58 个 / 748.9 MB**，**超期 12 个 / 7.9 MB**
- 分类：A 24个/21.7M（超期9）｜**B 2个/224.0M**｜C 29个/473.2M（超期3）｜D 3个/29.9M
- **体量 7 天涨 3.16 倍**（237→749MB），主要来自卡片文章 `_过程文件`（单目录 20–62MB）+ `_备份` 224MB
- 超期构成：A 类 9 个（`_预览`，7 天期，全部 7–10 天）+ C 类 3 个（19 天×2 封面候选、15 天×1 脚本归档）
- **C 类超期项已逐一核验为"可清"**（本次新增的关键前置检查）：
  - `孩子不上学了怎么办\公众号文章\_过程文件`（3 张候选图）→ 提示词存档已在该系列 `07_发布手册与转发文案.md` "封面意象与提示词存档"，**存档完整**
  - `为什么学生不喜欢上学\公众号文章\_过程文件`（3 张候选图）→ 同上，存档在 `02_发布手册.md` 第五节，**完整**
  - `工具脚本\_归档_20260905`（20 个一次性脚本）→ README 明示"已完成使命、可安全删除"
  - 注：两系列封面提示词均为"事后反推重建"（非逐字原始 prompt），手册中已如实标注
- 汇报方式：对话内文本汇报，**未生成任何文件**；未调用 present_files（无产物）
- 未执行任何删除/移动

### 2026-09-21（第三次执行 · 用户临时改保留期后重扫）
- **用户指令**：「把备份保留期缩短到 7 天，然后再扫描一遍」
- 改动 4 处并同步：①脚本 `POLICY["B"]` 14→7（+用法示例）；②标准版 SOP `10-ops.md` 保留期表与 `--keep-days` 示例；
  ③派生版 `_专家/Codex技能/.../references/maintenance.md`；④专家包 zip 重打包（**归一化比对 0 差异**）
- **C 类保持 14 天不变**（含提示词资产，不随 B 类同降）
- 原 zip 备份：`_备份/专家包/wechat-article-studio_改B类保留期前_20260921.zip`
- 变更记入 `_资产/SOP变更日志.md`
- **重扫结果**：仍 **超期 12 个 / 7.9 MB**（B 类无超期）；分类 A 24个/21.7M｜B 2个/**224.8M**｜C 29个/473.2M｜D 3个/29.9M
- ⭐⭐ **本次最重要的发现（机制局限）**：**B 类超期在汇总里显示 0，但这不代表没有超期项**——
  脚本对 `_备份/` 这类**容器目录**按其**内部最新文件**算龄 → 往里丢一个新快照，整个容器就"变年轻"。
  实测：容器报 **1 天**，而**内部 17/34 项已 7–19 天 / 99.1 MB**（`王金海讲书_断舍离与解码青春期…`46.9M/10天、
  `…爱的五种语言与非暴力沟通…`20.8M/10天、`…为何家会伤人与五顶学习帽…`14.1M/10天 等）。
  → **容器会把内部超期项一起藏住**；要查真实情况须**按顶层子项逐个算龄**。
- ⚠️ **未擅自改脚本统计逻辑**，只把局限写入 MEMORY + SOP变更日志 + 更新日志，**待用户决定是否让脚本按顶层子项统计**
- 只读性核验：`git status` 全为 `M`（修改），**无 `D`（删除）** → 确认零删除

### 2026-09-21（第四次执行 · 用户指示修 bug）
- **用户指令**：「修复脚本的bug，不要看目录时间，看文件真实最后修改时间」
- **修法**：
  1. **判龄口径显式化**：只看文件 `st_mtime`，**目录自身时间一概不看**（写进 docstring，标注"不可改"）。
     `stat_dir()` 改返回 `(size,cnt,newest,oldest)`；新增 `stat_file()`/`stat_any()` 统一文件与目录入口；
     空目录按"未知"(0) 处理，避免 `fromtimestamp(0)` 误算成 50 年。
  2. **新增 `is_snapshot_catalog()`**：识别快照总目录（名称以 `_备份` 开头，**或**含 ≥2 个带「公众号文章」层级的子目录）
     → 命中则**逐个快照单独判龄**，报告中**逐快照展开成一行**；`--brief` / `--list` 同步逐条列出。
- **效果**：`超期 12 个 / 7.9 MB` → **`超期 29 个 / 107.1 MB`**，原先被藏住的 **17 个快照全部现形**。
- **回归验证**：①57 个普通目录判龄与手工独立实现**逐一致**；②`--keep-days` 覆盖仍生效；
  ③脚本**无任何删除/移动/改名调用**（仅 `--out` 写报告）。
- **同步落点**：脚本 + 标准版 `10-ops.md` + 派生版 `references/maintenance.md` + 专家包 zip（**归一化 0 差异**）
- ⭐ **判据取舍过程（重要）**：先想用"非过程前缀子目录数 ≥2" → 实测**误报 16 个**（把 `_过程文件` 里的
  `卡片成品_待验收`/`封面样板_Gate3` 等**阶段目录**误判成快照）；改用"子目录内含「公众号文章」层级" →
  `_备份` 12/28 命中、其他 4 个过程目录**全 0 命中**，区分干净。
  **教训：判据要靠实测对比多个候选来选，不能凭直觉。**
- **可推广教训**：**凡"内含多个独立条目"的目录，判龄必须下沉到条目层**——
  用 `max()` 聚合会让"新条目"代表"老内容"，是**静默漏报**的典型来源。

### 2026-09-21（第五次执行 · 用户授权实际清理）
- **用户指令**：「清除按规则超期的文件吧」→ 走完整安全清理流程，**清理 30 项 / 108.3 MB**
- ⛔ **前置核查的关键发现**：要清理的目录**全部未被 git 跟踪** → **"先 git commit 兜底"本次完全无效**，
  **回收站是唯一退路** → 故先写 `_档案/报告/00_过期清理清单_20260921.md`（路径/体积/文件数）留档，
  因回收站恢复项无法独立枚举（SOP 要求此时必须披露）。
- 环境：系统 python **无** `send2trash`/`pywin32` → 必须用 **managed venv**
  `C:\Users\ZhuanZ\.workbuddy\binaries\python\envs\default\Scripts\python.exe`（两者齐备）。
- 执行：3 批（10+10+9）+ 1 项（`林文采关系课\_预览` 扫描后新跨阈值）= **30 项**
- ⭐⭐ **再次踩到"假报错"**：`send2trash` 大量抛 `FileNotFoundError`/`OSError` 但**实际已成功**。
  **唯一判据＝查回收站条目 + 查文件系统**，绝不凭异常判定。
- ⭐ 第 1 批 4 项「部分删除」（内容进回收站、留 2 个 jpg 残骨）→ **重试即收口**。
- ⭐ **目录级调用真失败时，降级为「逐文件 + 最后清目录」**：`林文采关系课\_预览` 目录级连试 2 次真失败
  （6 文件仍在），**逐文件调用全部成功**。→ 可推广的绕行手法。
- ⚠️ **清后自检一度 12 系列报 ❌，唯一失败项是【8】合并预览页**（`_预览/` 被清空所致），
  发布包本体 1–7、9 项全 ✅。→ **这是清理的预期副作用**；
  跑 `工具脚本/重建全部正文预览.py`（不传参自动发现全部系列）后 **23/23 系列全部通过**。
  **教训：清 `_预览/` 后必须先重建预览再跑自检**，否则会把预期副作用误判为"清理弄坏成果"。
- 结果：扫描 `超期 29 项 / 107.1 MB` → **`超期 0 项`**；过程类目录 `749.7 MB → 641.4 MB`

## 复用要点
- **汇报必须跑完整版**：`--brief` 只给汇总，列不出具体文件夹路径；完整版输出 A/B/C/D 四张明细表（体积/文件数/放置天数/路径）。
- 汇报硬要求：写**具体文件夹路径**，不能只报类别代号；并解释目录名模式含义（A/B/C/D 保留期）。
- **C 类超期前必须先查"提示词是否已摘录进该系列发布手册封面小节"**——不能只看"超期了"就报可清。
  本次核验方法：①grep 手册"封面"；②读手册封面存档小节确认有实质内容；③ls 确认过程文件夹里只有图无 prompt 文本。
- **⭐ 容器类目录（`_备份/`）必须另按顶层子项算龄**，否则内部超期项会被容器"平均年轻"掉。
- **改保留期要同步 4 处**：扫描脚本 POLICY + 标准版 SOP + 派生版 maintenance.md + 专家包 zip（重打包后归一化比对须 0 差异）。
  zip 重打包方法：读原 zip 全部条目 → 用 cache 最新内容替换同路径条目 → `os.replace` 覆盖。改前先备份原 zip 到 `_备份/专家包/`。
- **本任务恒为只读**（改保留期属配置修改，非删除）；实际清理须用户明确圈选后另行执行（回收站 + 分批 ≤10 项 + 逐批核对 + 清后复检）。
- 清理前应先 `git commit`，让删除可回滚。
- 环境坑：本机 Git Bash 下 `cp`/`head`/`grep`/`sed` 常 `command not found` → 改用 `python -c` 完成同为操作。
