# 项目长期记忆 — 家庭教育

> 工作空间 `D:\个人资料\家庭教育`。**只记红线 + 约定 + 高频硬坑**。
> **细则归口**：排版/卡片 → `工具脚本/卡片渲染避坑速查_说明.md`｜专家包 → `plugins/cache/my-experts/<包名>/1.0.0/`｜管道 → `_技能/ffmpeg-vertical-video-pipeline/SKILL.md`。
> **⭐ 排版三层防线**：①`04-html.md` R1–R5 生成 → ②项目自检 → ③官方检测 `wechat_official_verify.py`（权威）。⛔ 不能只靠③（22 章仅 15 章被查）。

## 一、内容红线

1. **类型分轨**（`02-writing.md`）：方法/概念文 → 五层齐全（场景·心理转折·机制·方法·**边界**）；叙事文可缺机制与方法，**但边界不可缺**。
2. **叙事真实性三分法（最高门槛）**：①真实经历→"我和儿子"；②课程案例/改编→**标"场景改编"**；③他人→第三人称。**红线：绝不把讲师自述改写"我"**；⭐ **不替孩子补台词**（素材里没有的孩子的话一句都不能补，**那是凭空生成**）。
3. **事实核验**：书中模型写"书中提出"、课程观点写"课程认为"；**研究/数字/履历核不到不写**；不写"必然导致"。
4. **人物连续性**：孩子固定 **15 岁/高一/儿子**；叙事者"一个也在重启的爸爸"；后台【作者】="归途有光·和孩子一起重启"。
5. **⛔ 正文禁例**：①绝对化归因+制造羞耻 ②密集引用不标出处 ③结尾居高临下给指令。
6. **过稿捷径警惕**：并列分论点做**"核心动作提取"**比对——同质即合并。
7. **卡片张数是"果"不是"因"**：先数长文独立核心意思，**3–5 张**（3 张常为正解）；**Gate A 写明依据**。

## 二、目录落位

**根目录只允许：内容系列目录 + `.git/` `.gitignore` `.workbuddy/` + 固定管理目录。**

- 文章/讲书系列 → **根目录平铺**；脚本 → `工具脚本/`（**不改名**，引用 29 处）。
- 管理类（`_` 前缀）：`_专家/` · `_资产/` · `_备份/<对象>/` · `_档案/报告/` · `_技能/<名>/` 技能快照（源在 `~/.workbuddy/skills/`）。
- 官方检测缓存 → `~/.workbuddy/cache/wechat-verify-spec/`（自动装，不进 git）。
- **移动文件必须同步改引用**；**新脚本名一律 ASCII**（⚠️ 存量中文名不改）。
- **过程目录留在系列内**，保留期由 `工具脚本/过程文件过期扫描_scan_stale.py` 管（**判龄只看文件真实修改时间**）。
- **⭐ `.gitignore` 用「前缀+`*`」**，禁精确目录名（曾致 60MB 误入库）；改后**必须 `git check-ignore -v` 实测**。

## 三、专家包与技能维护

- **专家包唯一真相源 `plugins/cache/my-experts/<包名>/1.0.0/`**；顺序固定：**改 cache → 同步 marketplace → 重打包 zip → 归一化比对**。
- **技能源在 `~/.workbuddy/skills/<名>/`，不热加载**；改完跑 `工具脚本/skills_sync.py` 同步进 `_技能/` 才入版本库。
- **改架构级规则必须全库 grep 旧关键词**（含 README/工具文档）；**改「规则语义」≠「词汇替换」**。
- **两包分工**：工坊=生产线；学习工坊=研发部（**⛔ 绝不自动改工坊 SOP**，须用户 Gate C 点名）。阅读量**不公开**；**P0：绝不编造"10万+"**。
- **⚠️ 纯词替换陷阱**：生图提示词里 `hands/table` 英文意为**"桌子"**。

## 四、交付与确认纪律

- **① `present_files` 不必每次调用**；默认只在文字里写清产物路径。例外：用户当次说"打开/看一下/预览"。
- **② "少交付"≠"少确认"**：**Gate 永远等用户当次、点名到这一步的指令**；"直接做/继续做"**不扩大到未提及的关卡**。
- **③ SOP 演进不追溯存量**：已生成的保持不变，除非当次点名返工。
- **④ 改完能力要「清点文档入口」**（含 frontmatter `description`——决定技能是否加载）；并验编号无重复。

## 五、账号事实

- 公众号**个人订阅号、未认证**（`wx148b8bbfe4e1dc52`）→ **无官方 API**，别试 `draft/add`。**发布=半自动**。
- **视频号已绑定公众号**（也未认证）。**合规两条**：①不得用利益换关注 ②不得导到微信外部。
- **同篇卡片版与长文版同日发布，先长文后卡片**；**载体判据**：明细表**有「完读率」= 长文版**。⛔ **两载体指标不可互比**。
- 运营细节见 `_资产/发布数据复盘.md`（通知额度一天一次；**真杠杆是"被转发"**）。
- **标题对照实验**（`_资产/标题对照实验_协议.md`）：A/B 各 ≥10 篇再比；判据含**不看阅读量**；**攒够前不写进 SOP**。

## 六、剪映（已排查）

**⛔ 路线B 不可行**——剪映**强制草稿为密文**（证据见 `_档案/报告/剪映路线B可行性结论.md`）。⭐ **成片已改走 ffmpeg 管道**；若取消加密可复活：`工具脚本/jianying_draft_inspect.py`。

## 七、视频号成片（ffmpeg 管道）

**⭐ 唯一真相源 = `_技能/ffmpeg-vertical-video-pipeline/`**（`SKILL.md` 用法/参数/硬坑；`reference/md_authoring_guide.md` 文案 MD 规范）。**本文件不复述细则**（曾因两处格式撞车）。

- **入口**：`video_make.py --md 文案.md --videos-dir _素材 --out 成片.mp4`（`01.mp4…` 文件名序＝段序，`_` 开头忽略）
- **文案目录**：`孩子不上学了怎么办/视频号文案/0N_xxx/`（MD ＋ `_素材/` ＋ `_过程文件/`）
- **⛔ 三条硬坑**：①滤镜里 `C:\` 被吃 → **`cwd` 到目录＋只传文件名**；②**`-i` 必须在 `-vf`/`-af` 前**；③**画面处理必须在 ASR 前**（否则字幕时间码错位）。
- ⛔ 改 `scripts/` 前先 grep 参数名/函数名（曾重复定义 `--md` 致 argparse 冲突）。

_最后更新：2026-09-24_
