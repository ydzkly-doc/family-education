# 项目长期记忆 — 家庭教育

> 工作空间固定 `D:\个人资料\家庭教育`。**本文件只记红线 + 项目约定 + 环境硬坑。**
> 细则以专家包 SOP 为准：`plugins/cache/my-experts/wechat-article-studio/1.0.0/`（工坊）、`wechat-learning-studio`（学习工坊）。
> **操作型踩坑（C24–C33、官方检测工具、批量修复纪律等）见 `工具脚本/卡片渲染避坑速查_说明.md`** —— 本文件有 3000 字符限额，细则不放这里。
> **⭐ 排版三层防线**：①生成阶段按 `04-html.md` **R1–R5** 写 → ②项目自检（**启发式**）→ ③**官方检测** `wechat_official_verify.py`（**权威**）。⛔ **不能只靠第③层**——规范 22 章仅 15 章被查，**7 章不查**（含 `font-family`）。

---

## 一、内容红线

1. **文章类型分轨**（`02-writing.md`）：方法文/概念文 → 场景·心理转折·机制·方法·边界**五层齐全**；
   叙事文 → 可缺机制与方法，但**必须有场景·心理转折·边界**。**⛔"允许缺方法"≠"可不写边界"**。
2. **叙事真实性三分法（最高门槛）**：①确认真实经历→可写"我和儿子"；②课程案例/改编→**第一屏标"场景改编"**；③他人案例→第三人称。**红线：绝不把讲师自述改写成"我"。**
   ⭐ **近亲变体**：**不替孩子补台词**——素材里没有的孩子的话，一句都不能补（补"我的状态/动作"可以）。与"孩子侧不细写"同源，但更隐蔽：**不是泄露，是凭空生成**。
3. **事实核验**：书中模型写"书中提出"、课程观点写"课程认为"；**研究/数字/履历核不到不写**；不写"必然导致"。
4. **人物连续性**：孩子固定 **15 岁/高一/儿子**；叙事者"一个也在重启的爸爸"；后台【作者】用账号名"归途有光·和孩子一起重启"。
5. **⛔ 正文禁例三条**：①绝对化归因+制造羞耻 ②密集引用不标出处 ③结尾居高临下给指令。
6. **过稿捷径警惕**：并列分论点须做**"核心动作提取"**比对——同质即合并或换角度。
7. **卡片张数按内容厚度定**：**张数是"果"不是"因"**——先数长文**独立核心意思**，区间 **3–5 张**，**3 张常为正解**。**Gate A 须写明张数依据**。

---

## 二、目录落位（强制）

**根目录只允许：内容系列目录 + `.git/` `.gitignore` `.workbuddy/` + 固定管理目录。**

| 产物 | 落位 |
|---|---|
| 文章/讲书系列 | 根目录平铺 |
| 工具脚本 | `工具脚本/`（**不改名**，被引用 29 处） |
| 官方检测缓存 | `~/.workbuddy/cache/wechat-verify-spec/`（自动装，不进 git） |
| 专家包及派生物 | `_专家/` |
| 长期资产 | `_资产/` |
| 备份 | `_备份/<对象>/` |
| 一次性报告 | `_档案/报告/` |

- `_` 前缀=管理类；移动文件**必须同步改引用**。
- **⭐ 新脚本名一律 ASCII**（中文名在终端/工具链下易乱码）；⚠️ **存量中文脚本名不改**（被引用 29 处）。
- **过程目录不要挪**：`_过程文件/` `_封面候选*/` `_预览/` `_归档*/` 留在系列内，由 `工具脚本/过程文件过期扫描_scan_stale.py` 管保留期（**A 预览 7 天｜B 备份 7 天｜C 素材 14 天｜D 归档不过期**）。**只有 `_backup*/` 归口 `_备份/`**。
  ⚠️ **判龄看「文件真实最后修改时间」，不看目录时间**；`_备份/` 这类快照总目录已改为**逐快照判龄**（整体判龄会被新快照"平均年轻"，曾藏 17/34 项）。**凡含多个独立条目的目录，判龄都要下沉到条目层。**
- **⭐ `.gitignore` 用「前缀+`*`」，禁精确目录名**（曾致 60MB 误入库）。⛔ 前缀必须与真实目录名**逐字一致**（`_backup*` 匹配不到中文 `_备份_更新前_*`）；`.` 开头目录要另写规则；改后**必须 `git check-ignore -v <真实路径>` 实测**。**四类反例见避坑速查文档。**

---

## 三、专家包维护（铁律）

- **唯一真相源 `plugins/cache/my-experts/<包名>/1.0.0/`**。顺序固定：**改 cache → 同步 marketplace → 重打包 zip → 归一化比对**。
- **改架构级规则必须全库 grep 旧关键词**：清扫范围**必须含 README/工具文档/"顺带提及"的描述句**。**改「规则语义」≠「词汇替换」**（"共5张"→"≤5张"不够，要补方法）。
- **⚠️ 纯词替换陷阱**：生图提示词里 `hands/table` 是英文**"桌子"**。
- **两包分工**：工坊=生产线（素材→文章）；学习工坊=研发部（好文章→技法→反哺 SOP）。
  学习工坊 **⛔ 绝不自动改工坊 SOP**：技法先入库 → 用户 Gate C 逐条点名 → 才动手。
  硬约束：阅读量**不公开**，只用替代信号分级；**P0：绝不编造"10万+"**。
  拆解篇目最佳搭配=「优质特稿+典型爆款套路」；政务/学校号"现象+原因+N条建议"套娃**无技法可学，排除**。

---

## 四、交付与确认纪律

- **① `present_files` 不必每次调用**。以用户明确指令为准；**默认只在文字里写清产物路径**。唯一例外：用户当次明确说"打开/看一下/预览"。
- **② "少交付"≠"少确认"**。**Gate 1/2/3 永远等用户当次、明确、点名到这一步的指令**；"直接做/继续做"**不得扩大解释为对未提及关卡的授权**。
- **③ SOP 演进不追溯存量**：后续按新 SOP 执行，**已生成的保持不变**；冲突默认不动，除非当次点名返工。
- **④ ⭐ 改完能力后要「系统性清点文档入口」**（用户已**两次**追问"更新到 skill 了么"，两次都查出真遗漏）：
  逐项 grep 核对，至少覆盖 **frontmatter `description`（⛔ 最易漏，它决定 Agent 是否加载技能）/
  何时用 / 管道构成 / 前置条件 / 快速开始 / 专属章节 / 快捷开关 / 作业流程 / 参数速查 /
  自检入口 / 硬坑清单 / 目录 / reference 规范 / 示例文件 / `--help` epilog / 项目文档**。
  ⛔ 光写"专属章节 + 硬坑 + 目录"**不算更新完**——上次就漏了 6 处。
  ⚠️ 同时验 **硬坑编号连续无重复**、**章节无重复**（曾因多方编辑撞出重复编号）。

---

## 五、账号事实

- 公众号**个人订阅号、未认证**（AppID `wx148b8bbfe4e1dc52`）→ **无官方 API 权限**，不要再试 `draft/add`。**发布=半自动**。
- **视频号已与公众号绑定**（也未认证）→ 绑定门槛是「同管理员+实名一致」，**不是认证**。主页已常驻公众号入口。**合规边界只两条**：①不得用利益换关注 ②不得导到微信外部。
- **运营事实详见 `_资产/发布数据复盘.md`**：通知额度一天一次、同日多篇共享 → 「送达人数」**不能评价单篇**；触达靠**推荐与转发**；**真杠杆是"被转发"不是"被关注"**。
- ⭐ **发布机制（2026-09-23 用户确认）**：同篇的**卡片版与长文版同日发布，先长文后卡片**（卡片版原文链接指向长文版）。
  **载体判据**：明细表**有「完读率」字段 = 长文版**（无 = 卡片版，推荐占比 64–95% 交叉确认）。
  ⛔ **同篇两载体的任何指标都不可互比**（已因此翻车一次）。
- ⭐ **标题对照实验已立项**：A 组全用正式标题、B 组全用备选，各攒 ≥10 篇再比；
  **两载体须用同一类标题**；判据=停留/完读率/分享/评论（**不看阅读量**）；**必记推荐占比**。
  细则与风险见 `_资产/标题对照实验_协议.md`；**攒够前不得写入 SOP**。

---

---

## 六、剪映（外部工具）· 已排查结论

**⛔ 剪映自动化「写草稿文件」路线（路线B）在 11.5 上不可行**——剪映**强制要求 `draft_content.json` 为密文**，
写明文会被静默拒绝（对照实验已证实，唯一变量=明文/密文）；**且加密不可配置**。
完整证据链见 `_档案/报告/剪映路线B可行性结论.md`。
- **复查方法**（未来版本若取消加密即可复活）：`python 工具脚本/jianying_draft_inspect.py <草稿名>`
- 草稿目录 `D:\Program Files\JianyingPro Drafts`；**草稿列表显示名取自文件夹名**
- 剪映自带 ffmpeg（`11.5.0.14471\ffmpeg.exe`）**无 libx264、无 subtitles/ass/drawtext** → 不能烧字幕
- 「视觉+坐标+输入注入」操作 GUI 可用（脚本见 `工具脚本/jianying_zorder_probe.py` 等）

---

## 七、视频号成片管道（ffmpeg 全自动，不走剪映）

**✅ 完整版 ffmpeg**：`C:\Users\ZhuanZ\.workbuddy\binaries\ffmpeg\bin\ffmpeg.exe`
（BtbN win64-gpl，含 libx264 + libass/freetype/harfbuzz/fontconfig）；
**剪映自带那个是阉割版**（无 libx264、无 ass/drawtext），**不能用**。

**⭐ 权威代码在用户级技能目录**（`工具脚本/` 下 4 个 `video_*.py` 是历史副本，2026-09-24 起不再维护）：
`C:\Users\ZhuanZ\.workbuddy\skills\ffmpeg-vertical-video-pipeline\` —— `SKILL.md`（用法/参数/硬坑 15 条）
＋ `scripts/`：`paths` `selftest`（**34 项一键自检，不需外部素材；改代码先跑它**）
`video_build_ass` `video_asr` `video_audio` `video_visual` `video_make`。

**⭐ 输入自适应 1~N 段**（段数不固定）：
- **N 个文件**（分段拍）⭐ 首选：`--video a.mp4 b.mp4`，**每个文件=一段，顺序即段序**
  → ①拼接(归一化+叠化+色温对齐+降噪) → ②剪停顿 → ③ASR…
- **1 个文件**：按「段间长静音」自动切 → ①剪停顿(保留段间停顿) → ②切段+叠化 → ③ASR…
- ⚠️ 单文件靠静音推断**会受底噪影响**（-35dB 检不出就静默退化成 1 段）→ 有 `health_check` 提醒

**⭐ 卡片位置一律用锚点，不写秒数**（秒数素材一变就错位）：
`"anchor":"某句话"`（推荐，容忍标点与错字）/ `"seg":N`（第 N 段）/ `"at":"end"` / `"start"`（最不推荐）；
配 `hold`/`lead`/`offset`。`punch`/`cover` 可给单对象或数组（多张卡）。
定位失败会打印原因+可用字幕行；**多卡重叠自动错开**（重叠=文字叠字）。

**⭐ 字幕三种版本**（`spec["subs"]`，不必全程都有字幕）：
`"all"` 全程 ｜ `[{"anchor":"某句"}]` **只上关键金句**（*只写锚点，字幕照抄原句、时长跟随原句*）
｜ `"none"` 纯卡片 ｜ `{"keep":["词"]}` 关键词筛选。
`subs_emph:[{"anchor":"某句","style":"强调"}]` = 给指定句加大加粗（即「全字幕+金句突出」）。
样式名：`字幕`(62px底部) / `强调`(78px加粗底部) / `序号条` / `金句`(100px居中)；
`to_end:true` 让卡片一直显示到片尾（配 `at:"end"` 做"结尾回放一帧方便截图"）。

**⭐⭐ 文案 MD 写作规范**（给创作者的那份要求）：
`skills/ffmpeg-vertical-video-pipeline/reference/md_authoring_guide.md`
三硬规则：**①⛔不写时间码（改说"挂在哪句话上"）②用「」把要上屏的原话逐字抄准
③写清版本（全字幕/金句版/全字幕+金句强调）**。
MD 里固定放 `## 上屏方案` 区块：版本 → 强调句 → 金句大字卡（含"挂这句"）→ 序号条（含"挂句"）
→ 全片标色 → 封面。**创作者不写 JSON，管道读 MD 生成剪辑单**（附对照表）。

**⭐⭐ 文案 MD → 成片（推荐入口，2026-09-24 加）**：
`python scripts/video_make.py --md 文案.md --videos-dir _素材 --out 成片.mp4`
- **素材约定**：`01.mp4 02.mp4 03.mp4`，**按文件名排序 = 段序**；`_` 开头文件忽略
  （成片可直接放同一层，不会被当成素材）
- `--md` 自动解析「## 上屏方案」（版本/强调句/大字卡/序号条/标色/封面），
  并把逐字稿抽到 `<md名>_文案.txt`
- ⭐ 三个"**不合成也能看**"的开关（都不校验素材）：
  `--dump-params` 看解析结果 ／ `--show-chain` 看滤镜链 ／
  **`--preview` 看上屏落点表**（用逐字稿模拟时间码，素材没拍也能验，省拍完返工）
- 项目文案目录：`孩子不上学了怎么办/视频号文案/0N_xxx/`（MD ＋ `_素材/` ＋ `_过程文件/`）
- 金句版实测**抗偏离**：改词/漏句/合并/加话下，6 处挂句仍 6/6 全命中

⛔ **改 `scripts/` 前，先 grep 要加的 CLI 参数名与函数名**——曾因重复定义
`--md`/`--videos-dir` 造成 argparse 冲突，脚本一度完全跑不起来（详见当日 log）。

⛔ **画面处理必须在 ASR 之前**（转场缩短总时长，否则字幕时间码整体错位）

**⛔ 硬坑（详见当日 log 与 SKILL.md 的 15 条）**：
1. ffmpeg 滤镜里 Windows 路径 `C:\` 会被吃掉 → **`cwd` 切到文件目录、只传文件名**
2. **CJK 满宽字符必须自动折行**，否则两侧被裁
3. `sys.stdout` **不可双重 TextIOWrapper** → 用 `reconfigure`
4. ASS 颜色是 **BGR 倒序**：`#C2703C` → `&H003C70C2`
5. **`arnndn` 后必须加 `aformat=sample_fmts=s16`**（RNNoise 480 样本分帧与 AAC 不兼容）
6. **真峰值要靠 `alimiter`**，限制点比目标低约 1dB；`alimiter level` 必须 **false**
7. **`loudnorm` 分析结果必须校验**（极端素材吐出 `input_tp=5825` 会让命令失败）
8. **`xfade` 各输入格式必须一致**，且「统一」**必须内置在拼装函数里**（外包给调用方会漏）
9. **所有 `-i` 必须在 `-vf`/`-af` 之前**（已踩两次）
10. **多张卡片时间重叠 = 两条 Dialogue 同时显示 = 叠字** → `resolve_overlaps` 自动错开

**音频目标**：短视频平台 **I=-14 LUFS / TP=-1.5 dBTP**（实测产出 -14.70 / -1.59）
**RNNoise 模型**：`binaries/ffmpeg/models/arnndn/*.rnnn`（已备 6 款，默认 `bd.rnnn`）

**⭐ BGM 轨道**（2026-09-24 加，**默认关闭**）：`spec["bgm"]` + `video_bgm.py`（自检 57 项）
- ⭐ **位置是参数**：`placement`＝`头尾`（默认，只垫无口播处）/ `全程`（整片垫底＋人声段压低）
- 来源：`file` 指定 / `style` 按风格从曲库筛（`_资产/bgm/` ＋ `曲库.md`）
- ⛔ 找不到曲子、头尾没地方垫 → **报错停下**（绝不静默产出"没 BGM 的成片"）；**不联网下载**（版权）
- ⭐ **混音必须在 loudnorm 之前** → 两阶段：Stage A 混音落 WAV → 测量 → Stage B 上 loudnorm
- ⛔ **`sidechaincompress` 在本机构压不动**（实测仅 2.5dB）→ 自动压低改用 **ASR 人声区间音量包络**
- 头尾模式靠剪停顿 `keep_edges` **保留片头尾静音**（那是 BGM 落点）
- **默认值判据**：需要「额外素材」的**默认关**（punch/cover/hook/bgm）；对「已有素材」处理**默认开**

**⭐ 上屏元素与画面**（2026-09-24 补齐）：`subs`＝字幕｜`punch`＝**居中**大字卡｜
**`cards`＝浅底文字卡**（样式 `小卡`，可 `"style":"引用"`）｜`seq`＝序号条｜`hook`＝开头大字钩子；
**`zoom`＝画面轻微推近（Ken Burns，整片 1.0→1+zoom）**
- ⛔ `BorderStyle=3` 的底板色取自 **`OutlineColour`**（不是 BackColour）；此时 `outline` 是**内边距**
- ⛔ `zoompan` 必须给 `d=1` ＋ `fps=` ＋ `s=`；顺序必须在 `ass` 之前（否则字幕跟着放大）
- ⛔ `ffmpeg -i 文件` 仅探测**必返回非零码** → 探测类用**裸 `subprocess.run`**
- ⛔ 卡片类（punch/cards/seq）在 MD 里**必须用 `1.` 有序列表**（`-` 开头解析器不认）
- ⛔ 加元素/能力时**先 grep 三处**：`ELEMENT_KEYS`（安全区检查/渲染/自检共用）+ `md_spec` 解析 + `[md]` 摘要行

---

_最后更新：2026-09-24_
