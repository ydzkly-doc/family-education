# 2026-09-14 工作日志

## 新建「公众号学习工坊」专家包（wechat-learning-studio）

用户："我想做一个公众号学习agent，重点学习好文章的写作技巧，然后沉淀到 公众的工作坊中，你来引导我一步步完成吧"
→ 我先盘点现状、实测可行性、再逐关确认方案，最后建包。

### 定位判断（重要）
- 现有 `wechat-article-studio`（公众号爆款工坊）= **生产线**（素材→文章）
- 缺口：工坊 SOP 自己写着"规则要能被数据推翻，20–30 篇后再定稿"，但它**只有"写出去"的闭环，没有"学进来"的通道**
- 新包 = **研发部**（好文章→可迁移技法→反哺 SOP），是工坊的上游 + 反哺端，**不是替代品**

### 实测验证（动手前做的关键验证）
| 验证项 | 结果 |
|---|---|
| 搜索到公众号文章 | ✅ |
| **抓取 mp.weixin.qq.com 正文** | ✅ **实测拿到全文**（标题+开头+小标题+结尾完整） |
| 抓第三方转载（南方+、搜狐、头条） | ✅ |
| 获取真实阅读量/在看数 | ❌ **不公开**——公众号数据非公开，这是设计的硬约束 |

**由实测推出的关键设计**：因拿不到阅读量，改用**替代信号分级**
（多平台转载 > 被专业拆解号拆过 > 头部账号 > 用户指定；核不到就标 `证据：未验证`）。
⛔ 硬规则：**绝不编造"10万+"**。

### 用户拍板的方案（6 项）
1. 形态：**独立专家包**（不并入工坊，职责分离）
2. 来源：**agent 自动搜捕**（+ 用户投喂）
3. 筛选：**用替代信号筛**
4. 维度：**六维**——选题与角度（后加）/ 标题与开头钩子 / 结构与节奏 / 情绪曲线与共鸣点 / 金句与结论收尾
5. 反哺：**先入技法库 → 用户确认 → 才改 SOP**（不自动改）
6. 成果组织：**报告 + 技法卡双层**；技法库**按维度分类**

### 已建成
**专家包**：`C:\Users\ZhuanZ\.workbuddy\plugins\marketplaces\my-experts\plugins\wechat-learning-studio\`
- 架构**与工坊一致**：agent 骨架（常驻）+ `skills/wechat-teardown/`（入口 + 6 个按需细则）
- 6 个细则：01-candidates / 02-teardown（核心）/ 03-techniques / 04-library / 05-feedback / 06-maintenance
- **三档优先级**（P0 证据诚实·反哺须确认·不冒充原创·只读原始材料）
- **三道 Gate**：A 候选清单确认 / B 首篇样板确认 / C 反哺清单确认
- 头像：墨绿扁平图标（与工坊成对），水印已用邻近背景色修补（348 像素），512×512 / 211KB

**技法库**：`D:\个人资料\家庭教育\_资产\技法库\`
- `README.md`（索引+规则）、`00_技法总览.md`（速查）、`_模板_技法卡.md`、`_模板_拆解报告.md`
- 六维度目录 + `_拆解报告/`
- 技法卡 6 项必填，核心是**可迁移度**：`✅ 可直接用 / ⚠️ 需改造 / ❌ 不适用`
  - **`❌` 的卡同样要立**——记录"为什么这个爆款套路我们不用"
  - ⚠️ 第一人称技法陷阱：外部文章的"我"多是改写/虚构，搬到本账号**必须标改编**

- 校验 ✅ 通过；已注册进 marketplace；已打包 `_专家/wechat-learning-studio.zip`（237KB）
- **cache 目录暂未生成**（WorkBuddy 首次加载时才缓存，属正常）

### 关键边界（写给未来的自己）
- **本工坊不改工坊**：技法先入库、经用户确认、才动手；改则必须走 cache→marketplace→zip **三处同步**（2026-09-11 踩过漏同步的坑）
- 环境备忘：Bash 需 `export PATH="/usr/bin:/bin:/c/Windows/System32:$PATH"` 才能用 ls/find
- 隔离 venv 可用：`C:\Users\ZhuanZ\.workbuddy\binaries\python\envs\default`（Pillow 12.3.0 已装）

### 下一步（等用户指令）
- 阶段 3–6 待推进：试跑一篇（拿真实文章验证方法论）→ 接反哺接口 → 安装验证
- **停在 Gate 前等用户发话**，不抢跑

---

## 样板篇拆解完成（Gate B 停手）

用户确认：候选 **#1 + #2**、**五维完整**拆解。按 Gate B 纪律**只拆 #1** 作为样板。

### 候选筛选（搜了 4 轮）
- 搜到的**大多数是政务号/学校号说教文**（四川省政府、华亭二中、宁波外事学校、天河十八中、湘潭卫健委等）
- **筛选结论**：这类是"现象+原因+多条建议"的公文式套娃，**观点正确但无法法可学**，全部排除
- 留下 6 篇有技法价值的；推荐 **#1+#2 组合**（媒体特稿 vs 爆款套路，对照价值高）

### 拆解 #1《我的女儿，初二，不去上学了》
- **来源核实**：公众号实际发布方是 **36氪**，内容"经后浪研究所整理发布"；作者巴芮；2025-06-22
- **证据等级**：`多平台转载（≥5处）`——喜马拉雅音频改编、蜻蜓FM、播客《恩总的停学日记》引用、头条多篇改写
- 报告：`_资产/技法库/_拆解报告/2026-09-14_我的女儿初二不去上学了.md`

### 6 张技法卡（5 ✅ / 1 ⚠️）
| 卡 | 维度 | 可迁移度 |
|---|---|---|
| 用相对时间坐标开头 | 02 | ✅ |
| 标题用白描陈述不用标签词 | 02 | ✅ |
| 小标题用进程式取代分论点并列 | 03 | ✅ |
| 写"不体面的真实反应"替代正确情感 | 04 | ⚠️ 需改造（须过叙事真实性关） |
| 用"他人提问—自己回答"收尾 | 05 | ✅ |
| 结尾承认不确定不给完满结局 | 05 | ✅ |

### ⭐ 本次最重要发现（对工坊有挑战）
**"不给答案"的文章可以是极好的文章。**
- 工坊"五层完整性"（场景/心理转折/机制/方法/边界）是**为方法文设计的**；本篇**缺机制与方法**却仍极有力
- 说明**存在另一类叙事文**，标准应是"细节的诚实度 + 情绪的克制度"，而非"方法是否齐备"
- 已写入报告第 6 节标为"⚠️ 冲突，需讨论"——**这是 Gate C 最值得用户拍板的一条**
- 另：本篇**无情绪操纵**（不给廉价出口、不恐吓、承认"我不知道"），与账号克制调性高度契合

### 待办
- **停在 Gate B**：等用户确认样板（深度/角度/格式）后，才拆 #2（女儿派《闭嘴》）
- #2 是**典型爆款套路文**，拆它可检验可迁移度判定的判别力（预期会产出 ❌ 卡）
（2026-09-14 补记）

---

## 样板确认 → 拆解 #2 完成 → 停在 Gate C

用户："确认" → 样板通过，继续拆 #2。

### 拆解 #2《为人父母，懂得"闭嘴"才是最高层次的智慧》
- **来源核实**：女儿派（ID nverpai），作者叶丫丫
- **证据等级**：`多平台转载（≥4处）+ 头部账号`；**且有额外发现——出现洗稿版**
  （逐句改写但结构完全照抄，"会说话是本事，但懂得闭嘴"那句一字未改）
  → 说明**这个结构模板已被行业当作可复制标准件**，是套路成熟的直接证据
- 报告：`_资产/技法库/_拆解报告/2026-09-14_懂得闭嘴才是最高层次的智慧.md`

### 拆出 6 张卡：3 ✅/⚠️ + **3 张 ❌**（后者是重点）
| 卡 | 维度 | 判定 |
|---|---|---|
| 开篇用旁观者视角切入 | 02 | ✅ |
| 用具体事项罗列表现"唠叨" | 04 | ✅ |
| 小标题用"当…时"句式（去命令感改造） | 03 | ⚠️ |
| **用"全看父母的嘴"绝对化归因制造羞耻** | 04 | **❌** |
| **密集引用名人名言不标出处** | 03 | **❌** |
| **结尾"亲爱的…嘘，别说话"式居高临下指导** | 05 | **❌** |

### ⭐ 本次证明的关键能力：可迁移度判定的**判别力**
- #2 **在传播上确实有效**（多平台转载 + 被洗稿），但**与本账号根本冲突**
- 若判定只按"传播效果"排序 → 会全盘采纳 → 账号调性被污染
- **正确结果：3 ✅ / 1 ⚠️ / 3 ❌** → 说明"能不能搬到本账号"这个标尺真正起了过滤作用
- **❌ 卡的价值**：记录"为什么这个成熟套路我们不用"，是**反向护城河**

### ⭐ 两篇对照的核心结论
| 维度 | #1 特稿 | #2 爆款套路 |
|---|---|---|
| 姿态 | 我不知道 | 我知道，你照做 |
| 情绪驱动 | 摊开痛感不给出口 | 制造羞耻给极简出口 |
| 结构 | 进程式 | 并列式（**四个分论点同质**） |
| 结尾 | 承认不确定 | 亲昵的命令 |
| 转发动机 | 陪伴型 | **指责型** |
| 契合度 | 高 | 低（但零件可用） |

**一句话：#1 提供"精神"，#2 提供"零件"。本账号应用 #2 的技法零件，装进 #1 的精神框架。**

### 新发现的两个 SOP 缺口
1. **P0 级冲突**：工坊"五层完整性"（场景/心理转折/机制/方法/边界）是为**方法文**设计的；
   叙事文（如 #1）缺"机制+方法"仍极有力 → **需为叙事文定义独立标准**
2. **并列式分论点不得同质**：现有 SOP 无此检查项。#1 的四个分论点去掉场景差异后核心动作完全相同（都是"减少父母语言输出"）= 注水式并列

### 当前状态：**停在 Gate C**
- 技法库：2 篇 / 11 张卡（8 ✅⚠️ + 3 ❌）
- **7 条反哺候选已达触发门槛**，已列入 `00_技法总览.md`
- 按纪律**不自行改工坊 SOP**，等待用户逐条拍板
（2026-09-14 补记）

---

## 【Gate C 放行】7 条全部采纳 → 反哺工坊 SOP 执行完毕

用户："全部采纳" → 执行反哺（第 6 步）。**这是学习工坊首次反哺工坊 SOP。**

### 执行流程（严格按 05-feedback.md）
1. **备份**：`_备份/专家包/学习工坊反哺前_20260914/`（cache + marketplace + zip，29 文件）
2. **改 cache**（唯一真相源）：`references/02-writing.md`（主体 7 条）+ `agents/…md`（索引 + 2 条漏读点提醒）
3. **同步 marketplace**：cp 2 个改动文件
4. **重打包**：`_专家/wechat-article-studio.zip`（702.9 KB / 12 文件）
5. **三处归一化比对**：✅ **完全一致**
   - 坑：`.in_use/` 是运行时状态文件、`.gitkeep` 是打包器排除的空占位——比对时须排除，否则误报
6. **validate_expert.py** ✅ 通过
7. **10 项关键改动复验** ✅ 全在位
8. **留痕**：`_资产/SOP变更日志.md` 新增 2026-09-14 条目（含每条旧值→新值→实证依据）

### 写入的 7 条（全部落在 `02-writing.md`）
| # | 内容 | 性质 |
|---|---|---|
| ① | **五层完整性按文章类型分轨** + 叙事文三条替代标准 | 冲突裁决（最重要） |
| ② | 并列式分论点不得同质（含"核心动作提取"检查法） | 新增硬检查 |
| ③ | 小标题拟法：进程式 / 场景锚定式 | 新增 |
| ④ | 开头可用旁观者视角 | 新增 |
| ⑤ | 形容词 → 具体事项罗列 | 新增技法 |
| ⑥ | 叙事文收尾承认不确定 + 他问—我答 | 新增 |
| ⑦ | 正文禁例三条（附替代写法） | 新增·反例 |

### ⭐ ① 的关键设计（本次反哺的核心价值）
**"允许缺方法" ≠ "可以不写边界"**——叙事文可缺机制/方法，但**涉及厌学/自伤/抑郁时专业求助边界照样必须写**。
另：原"自查顺序固定为先保五层齐全"句已改为"先保五层齐全（**并按文章类型判断哪几层必需**）"。

### 技法库状态
- `00_技法总览.md`：**反哺记录区已填入 7 条**；待确认候选区已清空
- 全库：2 篇报告 / 11 张卡（8 ✅⚠️ + 3 ❌）/ 18 个 md 文件

### 下一步（等用户指令）
- 阶段 5–6 已完成（反哺接口 + 执行）。**阶段 6「安装注册 + 验证」**原本在建设路线图里，
  但学习工坊包**已在早前建成并注册**（见本日首条记录），故实为收尾。
- 可选项：是否继续拆新文章（回到第 1 步候选发现）、或创建定期自动搜捕的 automation
- **停在确认点，不抢跑**
（2026-09-14 补记）

---

## 先整理 MEMORY.md（系统提示超限被截断）

- 原 **9005 字符**（超限）→ 精简至 **6484**（-28%），备份 `_备份/MEMORY_精简前_20260914.md`
- **顺带修掉一处错误**：旧文第一节仍写"五层齐全（缺一不可）"，与当天刚改的 SOP（按文章类型分轨）**矛盾**——这正是"镜像 SOP"的风险。已改为分轨表述
- 重构为 7 节：内容质量总纲 / 目录落位 / 排版HTML / 发布包 / 专家包结构（含两个工坊）/ 自动发布 / 交付确认纪律

---

## 【新任务】用新 SOP 升级《为什么学生不喜欢上学》

用户："用新更新的工作坊sop升级一下 为什么学生不喜欢上学" → 先诊断，再按 Gate 确认方案。

### 诊断（跑了 `--diag-structure` + `--count`）
| 差距 | 表现 |
|---|---|
| **结构严重同质** | 8 篇里 **7 篇都是"读书概念解释型"**，六种结构只用到 2 种 → 违反"打破模具感" |
| **新技法未用** | 旁观者视角 0、进程式小标题 0、收尾承认不确定 0 |
| **结尾全同款** | 全是"5 条总结 + 祝愿收口 + 下篇预告"，相邻篇指纹雷同 |
| **事实风险** | 第7篇"一万小时"、第1/8篇"高考状元…相信自己" |

### 核实事实（P0，先做）
| 存疑项 | 结论 |
|---|---|
| "十年效应" | ✅ **书中确有此说**："一个人不花上十年时间是不能在任何领域成为专家的"。但**"一万小时"是外部引申，非本书原话** → 第7篇删掉 |
| "高考状元" | ⚠️ 王极盛（中科院心理所）**1999 起连续 10 年访谈 400+ 省级状元**确有研究；但"最一致是相信自己"**核不到**——公开结论强调**"坐得住、学得下"与心态** → 改为有据表述 |
| "乔丹 9000 次" | 名人自述 → 标注为"乔丹说过" |
| "绝大多数人智商相差不大" | ✅ 王极盛研究印证："大多数状元智商中等和中上" → 保留 |

### 用户拍板
- 范围：**结构轮换为主**（不做全面重写）
- 封面：**沿用现有 8 张**（不重新生成）

### 执行：改 4 篇结构 + 换收尾
| 篇 | 原 → 新结构 | 新收尾方式 |
|---|---|---|
| 4 知识先于技能 | 读书概念解释 → **问题复盘型** | 他人提问—自己回答 |
| 5 记忆是思考的残留物 | 读书概念解释 → **自问自答型** | 承认不确定 |
| 7 专家思维 | 读书概念解释 → **一封信型**（"一封没寄出的信"） | 停在场景里 |
| 8 成长型思维（收官） | 读书概念解释 → **问题复盘型** | 停在场景里 |

保留 1/2/3/6 篇（1 只修事实、6 已是对话拆解）。小标题全部改**进程式**（2–4 字动词/状态词连成心理进程线）。
**顺手修掉第5篇开头重复**（原稿把"抄十遍记不住"讲了两遍）。

### 校验结果
- `wx_html_fix --check`：✅ **合格 8/8**
- `--diag-structure`：✅ 结构已打开（问题复盘×2 / 对话拆解 / 书信 / 自问自答）
- 句式节流：✅ 命中 0/8
- `selfcheck --legacy`：✅ **总判定全部通过，可交付**
- 预览已重建；新增 `00_系列升级说明.md`
- 备份：`_备份/为什么学生不喜欢上学_新SOP升级前_20260914/`（48 文件）
- 逐篇字数降幅均 <12%（远低于 30% 复核线）→ **内容厚度完好**

### ⚠️ 未强行压缩篇幅（有意为之）
第 1/4/5/6/8 篇仍超 2400 字，但**原文本身就偏长**（第1篇改造前已 2607）。
按 SOP"区间是报警器不是配额，偏长不等于要删——先确认五层完整性"，**逐篇核过内容层无冗余，故保留**。

### 工具用法备忘（易错）
- `wx_html_fix` 接的是**文件路径列表**，不是 `--dir`（用 `find ... -name "正文_*.html"` 传入）
- 批量改 HTML 文字时，**文字被 `<span>` 切开**，简单字符串替换常不命中 → 改用**整段 `<p>` 正则定位**
（2026-09-14 补记）

---

## 【新任务】青春期30讲 13-18 篇规范升级（不动封面）

用户："更新一下青春期30将的13-18篇，不用更新封面图"

### 诊断（与上次不同：这次问题在**规范层**，内容层已良好）
13-18 篇是 9-10 改造过的（比 1-12 篇新），内容层检查：
- ✅ 无绝对化词、无动作指令、五层齐全
- ✅ 节流 0/6 达标、HTML 规范 6/6
- ✅ 结构已多样（含**书信型**第15篇、**自问自答型**第17篇）
- ⚠️ 第18篇偏长 2924 字（收官合集含 7 张小抄长卡，**判定合理，未压**）

### ⭐ 核心发现：全 18 篇季色跨季混用（遗留 bug）
- **标题块按季变色**：S1 橘 `#c0764f` / S2 雾蓝绿 `#5f8a86` / S3 金棕 `#c0924f`
- **但全 18 篇的竖条小标题都是 S1 橘 `#c0764f`** ← 第二三季改造时只换标题块、**漏改竖条**
- 违反 SOP"同一系列内颜色保持一致"

**⚠️ 中途踩坑与纠正**：我先按"13-18 篇有 bug"改了 14-18 篇，随后查第12篇(S2 参考)时发现**6-12 篇也一样** → 说明是**全系列问题**，不是 13-18 篇特有。**立即回滚**，停下来向用户澄清"这是设计还是遗留"。用户确认：**是 bug，该改**。

### 用户拍板
- 范围：**只修规范遗留**（不做内容优化）
- 第13篇：**改回第二季**（雾蓝绿）

### 执行
1. **14-18 篇**：竖条 `#c0764f` → `#c0924f`（S3 金棕）
2. **第13篇**：**整篇 S3→S2 换色**（9 个色值映射：主色/深色/浅底/边框/深字/中深字/深色块浅字/金句块底/竖条）
   - 依据：规划明确"第二季 · 做工作（第 6–13 篇）"，正文也写"第二季到这里就结束了"，**只有标题块误用 S3**
3. 换色后引入 B 类问题（颜色挂 section）→ 跑 `wx_html_fix` 修复模式处理

### 校验
- `wx_html_fix --check`：✅ **合格 18/18**
- 配色一致性（标题块=竖条）：✅ 13-18 篇全通过；**S1 橘残留 = 0**
- `selfcheck --legacy`：✅ **总判定全部通过，可交付**
- 预览已重建（18 篇）；新增 `00_13_18篇规范升级说明.md`
- 备份：`_备份/青春期30讲_13_18升级前_20260914/`（25 文件）

### ⚠️ 已报告但**确认不改**（用户 2026-09-14 决定）
**第 6-12 篇（第二季）有同样的竖条色混用**：标题块已雾蓝绿、竖条仍是 S1 橘。
→ 用户明确：**6-12 篇已经发布，不用更改**。
→ 系列配色现状：1-5 一致（橘）；**6-12 标题块雾蓝绿+竖条橘（已发布，保持原样）**；13-18 各季同色（本次已修）。

### 关键经验（值得复用）
**批量换色前必须先横向扫"同系列全部篇目"的色值分布**——我先只看了 13-18 篇就动手，差点把"全系列问题"当成"局部问题"。
**且换季色时必须同时换竖条色**（本次 bug 的根因就是当初漏换竖条）。
（2026-09-14 补记）

---

## 【整理】各系列遗留 `_backup*/` 目录归口到 `_备份/`

用户：先问青春期30讲两个备份能否挪 → 挪完后我全深度扫描发现全workspace有更多 → 用户："不是，你需要扫描全部" → 一次性归口。

### 全深度扫描结论：**两类东西，只有一类该挪**
读了 `工具脚本/过程文件过期扫描_scan_stale.py` 的策略表后确认：
| 类 | 目录模式 | 保留期 | 处理 |
|---|---|---|---|
| **A** | `_预览/` | 7 天 | 留置（可重建） |
| **B** | `_backup*/`、`_备份/` | 14 天 | **`_backup*/` 归口到 `_备份/`** |
| **C** | `_封面候选*/`、`_过程文件/`、`_未采用*/`、`_原始*/`、`_归档*/` | 14 天 | **留置不动**（由保留期机制管理） |
| **D** | `_档案/`、`_资产/` | 不过期 | 留置 |

**⚠️ 关键判断：C 类过程目录不能挪**——它们是**设计上就留在系列内**、由 `scan_stale.py` 按 14 天保留期管理的。挪走会破坏机制。

### 执行：挪了 14 个 `_backup*/` 目录（27+ 文件，约 4MB）
落位规则：`_备份/<系列名>/[公众号文章/]<原后缀>`（剥掉 `_backup_` 前缀）
- 为什么学生不喜欢上学 ×2、父母做到这点孩子会有惊人改变 ×2、青春期30讲/公众号文章 ×2、孩子不上学了怎么办 ×2（含 公众号文章 层级）、王金海讲书 ×4（为何家会伤人/幸福的方法/心流/爱的五种语言）、手机危机处理 ×1、解决孩子沉迷手机 ×1
- **每个都逐目录核对文件数**（前后一致才算成功）

### 验证
- 系列目录内 `_backup*` **已全部清空** ✅
- `scan_stale.py --brief` 仍正常工作（45 个 / 244.6MB，超期 0）
  - 副作用（良性）：B 类由 15 个 → **2 个**（工具只把 `_备份/` 顶层算 1 个，不再逐个数子目录）→ **正是归口想要的效果**

### 未动（有意）
- **C 类过程目录 20 个 / 125MB**（`_过程文件/`、`_封面候选/`、`_归档_*/` 等）**留在系列内**——受保留期机制管理，挪走会破坏 `scan_stale.py`
- 已存在的顶层 `_备份/xxx_新SOP更新前_日期/` 一族（历史命名，风格不统一）**本次未重命名**，避免连带风险
（2026-09-14 补记）

---

## 【清理】执行一次目录清理（用户："执行一次清理吧"）

### 只读扫描结论
- `scan_stale.py` 报**超期 0 个** → **没有"按时间该清"的东西**，都是非时间类清理项
- 但**不限深度扫描发现上一轮漏项**：`王金海讲书/五顶学习帽/公众号文章/_backup_data污染修复_20260910`（上轮 maxdepth 3 没扫到）

### 用户确认后执行（4 项）
| # | 动作 | 结果 |
|---|---|---|
| 1-3 | 清理 3 个 `__pycache__` | ✅ **已送回收站** |
| 4 | 残留 `_backup` 归口到 `_备份/` | ✅ 完成 |
| — | `_资产/技法库/01_选题与角度`（空目录） | **保留**（技法库六维目录，等首次立卡） |

### ⭐ 踩坑与解决：Windows 回收站 API 全被拦
1. **PowerShell `Add-Type`（Microsoft.VisualBasic）** → 被安全策略拦（"compiles and loads .NET code"）
2. **PowerShell `New-Object -ComObject Shell.Application`** → 被拦（"COM object instantiation can run arbitrary code"）
3. **`send2trash` legacy 实现** → 中文路径报 `Errno 3`（它要做 8.3 短路径转换，中文路径失败）
   - **根因**：`send2trash` 无 `pywin32` 时回退 legacy（`SHFileOperation`）
   - **解决**：`pip install pywin32` → 启用 `send2trash.win.modern`（`IFileOperation`，原生 Unicode）
4. **heredoc 传中文路径** → Bash 把反斜杠转成混斜杠（`D://个人资料//`）→ 改用**写脚本文件**执行

### ⚠️ 重要教训：`send2trash` 会"假报错"
- 删完后**仍抛 `FileNotFoundError`**，但**实际已成功进回收站**
- **不能凭异常判定失败** → 必须**双重验证**：①直接查文件系统 ②查回收站条目（`Shell.Application` Namespace(10)）确认原位置
- 已在回收站核实到今天 12:08 的 3 组条目 + 4 个 `.pyc`，原位置可追溯 ✅

### 最终验证
- `__pycache__` 残留 **0**；系列内 `_backup*` 残留 **0**
- `scan_stale.py --brief` 正常（44 个 / 244.5MB，超期 0）
- git status 仍 220 项（**待清理项均未被 git 跟踪**，`.gitignore` 已覆盖 `__pycache__/` 与 `_backup*`，删它们不影响版本库）
- 临时脚本 `_cleanup_pycache.py` 已删

### 环境新增
- 隔离 venv 已装 **pywin32**（`C:\Users\ZhuanZ\.workbuddy\binaries\python\envs\default`）——**Windows 回收站操作必须用它**
（2026-09-14 补记）

---

## 提交到 git（用户："更新到git"）

220 项未提交改动 → 分 5 组提交 + 推送成功（`0dd5386..1775e76`）。

### ⭐ 提交前拦下的两个问题（重要）
**1. `.gitignore` 有真实漏洞**：忽略规则用**精确目录名**（`_封面候选/`、`_封面原图/`），
但实际过程目录多**带后缀** → 绕过规则：
- `_封面候选_新SOP`（14MB）、`_封面原图_新SOP_20260912`（15MB）、
  `_封面样板_Gate3`、`_封面候选_写实版`、`公众号文章_预览/`
- 合计约 **60MB 封面原图/候选**会被误入库，**与 .gitignore 自己注释里"不进版本库"矛盾**
- **修法**：全部改为「前缀 + `*`」——`_封面*` / `_过程文件*` / `_预览*` / `*_预览/` /
  `_原始png*` / `_未采用候选*` / `_归档*`（保留 `_backup*`）
- ⚠️ **踩坑**：我一度加了 `_拆解报告*`，但 `_资产/技法库/_拆解报告/` 是**正式成果**
  （拆解报告 + 模板），差点误伤 → 已移除该条并在 .gitignore 里写明原因

**2. 52 个历史已跟踪的过程文件**（`.gitignore` 管不到已跟踪文件）→ 用户确认后
`git rm --cached` 出库（**磁盘文件保留**）：`工具脚本/_归档_20260905`（20 个）、
`_封面原图`(12) / `_封面原始`(7) / `_封面候选_写实版`(6) / `_未采用候选`(4) / `_预览`(1)
→ 待入库文件 **322 → 282**。注：历史提交里的仍在历史中，`.git` 体积不变。

### 5 组提交
| 提交 | 内容 |
|---|---|
| `f7aeefa` | 修复 .gitignore 漏配；52 个历史过程文件出库 |
| `6e5d8c7` | 新建学习工坊专家包；技法库落库；7 条技法反哺 SOP |
| `2ab32c4` | 按新 SOP 升级两系列（为什么学生不喜欢上学 / 青春期30讲 13-18） |
| `b555b8a` | 王金海讲书 9 个存量系列升级 + 新增 2 个系列（231 项，最大一批） |
| `1775e76` | 封面更换、手机危机处理第2篇、Codex 派生技能同步 |

### 判断要点（值得复用）
- **`_build_preview.py` / `_render_upgrade.py` 是正式工具，不忽略**——全项目 19 个已在版本库，
  与 `_预览/`（一次性产物）性质不同。别被 `_` 前缀误判。
- **王金海讲书的改动是实质内容升级**（补"家庭场景改编"声明、把绝对化归因改为"书中提供的
  观察"、删核不到来源的"畅销几百万册"）——不是换色，属正式成果。
- **`_专家/Codex技能/` 是派生版**，改的正是今日 7 条反哺的落地表述，必须与主包同步提交。

### ⚠️ 环境怪象：`origin/main` 跟踪引用写不进去
- `git fetch` 报 `[new branch] main -> origin/main`，但 `refs/remotes/origin/main` **始终不落地**
  （`update-ref` 报成功也没文件；`for-each-ref refs/remotes/` 输出为空）
- `git status -sb` 一直显示 `## main...origin/main [gone]`
- **已确认无害**：`git ls-remote` / `push --dry-run`（Everything up-to-date）/ `fetch --dry-run`
  全部正常，远程与本地 commit 一致（均为 `1775e76`）
- 结论：**仅显示层问题**，`git push origin main` 显式指定远端即可，不影响功能。
  ⚠️ 排查时**不要用 `git credential fill`**（会明文回显 token）。

### 工具用法备忘
- `git commit -F <file>`：此环境 **`/tmp` 不可用**，临时 commit message 要写到项目内
  （用 `_档案/报告/` 下，`_档案/` 已被 .gitignore 覆盖、不会被误提交）
- `git status` 中文路径乱码 → `git config core.quotepath false`（已设为本地默认）
（2026-09-14 补记）

---

## 统一预览目录命名为 `_预览/`（用户："统一把"）

### 扫描结果：只有 1 个异类，不是多处
全库扫描 `*_预览` 目录 → **21 个全部是标准 `_预览/`**，只有 **1 个异类**：
`王金海讲书/刻意练习/公众号文章_预览`（13:44 遗留，9-13 生成）。

### ⭐ 关键判断：不是"改名"，而是"旧版残留"
异类目录与**同系列已有的标准目录** `公众号文章/_预览/` **三个文件全部同名**：
| 文件 | 异类版 | 标准版 |
|---|---|---|
| 全部正文预览 | 2.3 KB（iframe 外链，标注"封面尚未制作"） | **41 KB**（内联全文 + 导航/键盘切换） |
| 封面预览 | 2.4 KB | 2.3 KB |
| 前两篇样稿 | 1.4 KB | 1.3 KB |

**逐文件核对确认无独有内容**：封面预览两版内容**完全相同**，仅路径写法不同
（异类多一层 `公众号文章/`，**在标准位置下会失效**）；全部正文预览篇目数相同
（5 篇）、标题逐字一致。**标准版是更新更完整的那个**。
→ 故"改为 `_预览/`"技术上做不到（目标已存在）→ 实际处理是**删除旧版残留**。

### 处置
- 用户确认后**走回收站删除**（`send2trash.win.modern`），**又遇假报错**
  `FileNotFoundError [WinError -2147024894]` → 按既定方法**双重验证**：
  ①文件系统已不存在 ✓ ②回收站查到 13:58 的 4 个条目、原位置
  `王金海讲书\刻意练习` 可追溯 ✓
- 标准目录完好（3 个文件）；`.gitignore` 的 `*_预览/` 规则**改为防御性保留**
  （加注释说明：历史错误命名，已统一，留着防复发，不是让 `_预览*` 通吃）

### ⭐ 根因排查（防止复发）
检查全部 20 个 `_build_preview.py` 的输出路径 → **全部写 `_预览`**，
**根因不在脚本**；该异类目录是**手工生成**的历史遗留，不会自动再生。
→ 现有机制已能保证新系列命名正确，无需改脚本。

### 复核
- 全库有效 `_预览/` 目录 21 个，后缀式 **0 个** ✅
- `.gitignore` 回归测试：`*_预览/` 仍拦得住；`_预览/` 正常忽略 ✅
- 正式成果 `_资产/技法库/_拆解报告/` 未受误伤 ✅
（2026-09-14 补记）

---

## 清理「table 布局」残留表述（用户报告主 SOP 有一句冲突）

用户："主sop中文件里还残留一句'正文内只用 table'，与现行'禁止 table、统一 section 流式布局'相互冲突"

### ⭐ 实际不止 1 处——专家包内共 6 处，工作空间另有 4 处
**专家包（cache/marketplace/zip 三处同步）**：
| # | 位置 | 原文 → 改为 |
|---|---|---|
| 1 | `agents/wechat-article-studio.md:101` | 只用 table+内联样式 → 一律 `<section>`+内联样式（禁用 table）|
| 2 | `README.md:18` | 卡片用 `<table>`+内联样式内嵌 → `<section>`+内联样式绘制 |
| 3 | `01-planning.md:14` | 用 table 卡片正经呈现 → **section 卡片** |
| 4 | `02-writing.md:59` | 用 table 卡片正经呈现 → **section 卡片** |
| 5 | `05-publish-kit.md:31` | 卡片全是 table+内联样式 → `<section>`+内联样式 |
| 6 | `05-publish-kit.md:59` | 正文 table 可转为微信图床 → 正文 section 卡片可转为位图 |

**工作空间现行文件 4 处**：`为什么学生不喜欢上学/02_发布手册.md`、
`青春期30讲/00_选题规划_v6_终版.md:249`（"全部 table+内联样式；680px 主表"）、
`青春期30讲/03_发布手册.md`（2 处）。

### ⭐ 最严重的发现：`工具脚本/微信公众号排版HTML规范.md` 自相矛盾
它**顶部**（第 3 行）声明"布局改用 section、禁用 table"，但**下面四节仍完整按 table 写**：
- 「二、结构铁律」还在描述"外层满宽 table → 680px 主表"
- 「三、标签白名单」竟写着"**允许**：table / tr / td / p / span / b / strong / br"
- 「五、信息流」写"方法/对照/流程 **table 卡**"，「六、自检」写"table/tr/td/p/span/b 配平"

→ 已把四节全部改写为 section 版（白名单改为 `section/p/span/b/strong/br`），
并把顶部那句"table 骨架描述已被本节取代"改为"本文件自第 3 行起已全部改为 section 版"。

### 有意不改（重要判断）
- `05-publish-kit.md:43` 生图提示词里的 **`hands/table`** —— 英文"桌子"（提示模型别裁掉手/桌），**不是 HTML 标签**，改了就毁提示词。
- 各处 `零 <table>` / `严禁 <table>` / `取代旧 table 2号写法` / `table转section_*.py` 脚本名 —— **正确的禁止性表述与工具名**，保留。
- `_专家/Codex技能/`（派生版）：排查后**本来就正确**（第 49 行"不用旧 table 固定宽布局"）+ origin.md:8 是迁移历史说明，均不改。
- 各系列 `02_发布操作指南.md`（**58 个**）：已停止生成的**历史交付快照**，按既有纪律不反向修改。

### 根因与教训
这批表述来自"table 骨架时代"（2026-09-05 前）。9 月改 section 流式时**只改了规范条文主体，散落的描述性语句没同步清扫**。
→ **教训：架构级改动的清扫范围必须包含 README、「顺带提及」的描述句、以及同目录的配套工具文档，不能只改主规则。**

### 校验
- cache/marketplace/zip **三处归一化比对 ✅ 完全一致**；9 项改动复验 ✅ 全通过
- `validate_expert.py` ✅ 通过；zip 重打包 703.1 KB
- 全库最终扫描：现行 md 无残留（剩余命中仅为历史归档报告与本人变更日志）
- 备份：`_备份/专家包/table残留修正前_20260914/`；变更日志已留痕
（2026-09-14 补记）
