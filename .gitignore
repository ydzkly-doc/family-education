# 系统垃圾文件
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
*~
*.tmp
*.bak
# ⚠️ 2026-09-20 补：`*.bak` 匹配不到带**时间戳后缀**的备份（实为 `xx.py.bak-20260919-185636`）——
#    本次提交前抓到 6 个此类文件（约 190KB）差点入库。同类教训见下方 `_备份*` 那条。
*.bak-*

# 临时/预览缓存
~$*
*.swp
__pycache__/
*.pyc

# ===== 过程文件（2026-09-10 追加，不进版本库）=====
# 说明：以下均为「生成过程的中间产物 / 备份快照 / 派生文件」，不是正式成果。
#   _预览*       预览合并页（由 _build_preview.py 随时重建）
#   _backup*     各类备份快照（修复前、换色前等）——含目录与 zip，故不加尾斜杠
#   _归档*       一次性调试脚本归档
#   _过程文件*   从发布包挪出的过程脚本与未采用候选图
#   _原始png*    AI 生图带水印原图（成品封面已另存 封面_第X篇.jpg）
#   _封面*       所有封面过程目录（候选/原图/原始/样板/写实版）——成品是 封面_第X篇.jpg
#   _未采用候选* 未采用的封面候选图
#   _tmp*        一次性临时目录（描述草稿/放大目检图/符号对比图）
#                2026-09-16 补——当时 `_资产/_tmp_card3~5` 未被忽略，差点随提交入库
_未采用候选*
_tmp*
# ⚠️ 一律用「前缀 + *」而非精确名：历史上出现过后缀变体（_封面候选_新SOP、
#    _封面原图_新SOP_20260912、_封面样板_Gate3），精确名会漏掉它们（2026-09-14 修）
# ⚠️ 注意：`_资产/技法库/_拆解报告/` 是**正式成果**（拆解报告 + 模板），
#    不可用 `_拆解报告*` 全局忽略——故本清单不含该条。
_backup*
# ⚠️ 2026-09-18 补 **中文** `_备份*`：历史规则只写了英文 `_backup*`，
#    而实际目录名是中文「_备份_更新前_20260918」——**一直没被忽略**（本次提交前抓到）。
#    教训：规则里的前缀必须与**真实目录名**逐字一致，中英文混用会静默漏掉。
_备份*
_归档*
_过程文件*
_预览*
_原始png*
_封面*
_未采用候选*
# 2026-09-18 补：npm 缓存目录（`.` 开头，`_tmp*` 匹配不到；实测 58MB）
.tmp-npm-cache/
.tmp-*/
# 2026-09-18 补：任意位置的「备份 zip」（如 _专家/xxx.backup-20260916-*.zip）。
# 按约定备份应归口 `_备份/<对象>/`，不应留在成果目录里被提交。
*.backup-*.zip
# 说明：脚本目录内的「更新前备份」形如 `卡片文章/脚本/_备份_更新前_YYYYMMDD/`，
#      已由上方 `_备份*` 覆盖——它同样属回退快照，不是正式成果。
# 防御性规则：`*_预览/` 是历史错误命名（如 公众号文章_预览/）。2026-09-14 已
# 统一为 `_预览/`，现存目录已无此写法；保留此条以防再次出现。
*_预览/

# ===== 管理目录（2026-09-10 根目录整理后）=====
#   _档案/   一次性报告、验证小样等归档产物（可再生，不进版本库）
#   _备份/   各类备份快照（改前副本，不进版本库）
#   _专家/   专家包及派生物（需版本管理，不忽略）
#   _资产/   长期资产：真实素材池/发布数据复盘/SOP变更日志（需版本管理，不忽略）
_档案/
_备份/
