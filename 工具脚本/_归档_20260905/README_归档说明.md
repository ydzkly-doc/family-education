# 归档说明 · 2026-09-05

本目录收存从项目根目录 `D:\个人资料\家庭教育\` 归档来的一次性 Python 临时脚本（9/3–9/4 做公众号微信规范化批量处理时产生）。

- 这些脚本均已完成使命，**一般不需要再运行**；如后续要回顾当时的处理逻辑，可在此查阅，可安全删除。
- **日常请改用通用工具**（在上级目录 `工具脚本/`）：
  - 微信正文规范体检/修复：`工具脚本/微信HTML规范校验修复_wx_html_fix.py`（体检加 `--check`，无参则自动修复）。
- 各系列成果文件夹内的 `_build_preview.py`、`_recolor.py`、`_gen_*.py` 等构建脚本仍留在各自成果目录（随对应系列走），不在本归档内。
